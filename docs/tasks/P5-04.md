# P5-04 内核与 I/O 实验隔离（计划 §9.6；P5-03 移交 Buffer 复用 spike 承接）

状态：**CLOSED（全候选有据跳过，2026-08-25）**——阶段 1 复评判定 Buffer 池化进入条件失效（HEAD f4ae57f 分配计数重测 -70.9%，P5-03B 效应），按计划 §9.6 进入条件与「无收益即删除代码、结论留台账」停止规则收口：四候选全部有据跳过，spike 不实现，RED 契约保留为候选规格。详见「阶段 1 复评与收口决定」节；台账见 `docs/reports/performance/hotspot-ledger.md`（P5-04 收口登记）。
基线：main @ f4ae57fc993d71bb7fd865a5d4a2a415471da3b7（`docs(perf): record hotspot optimization ledger`——P5-03 循环收口提交；`git rev-parse HEAD` 复核一致）
依赖：P5-02 `VERIFIED`（方法学冻结 + gprof profile + CV 分母）+ P5-03 循环收口（A FAILED/REVERTED、B PASS/已提交 3fb46bb、C FAILED/REVERTED、D 评估不实施；台账「移交 P5-04」行：Buffer 分配复用候选移交本卡）
计划：`docs/plans/post-p2-implementation-plan.md` §9.6（spike 协议 + 候选进入条件全文）+ §9.8/§9.9（冻结值清单：spike feature flag 名与进入条件；停止规则：无 profile 证据不得开工 spike 实现、无收益 spike 代码不得留默认路径）
台账：`docs/reports/performance/hotspot-ledger.md`（P5-03 循环结论「移交 P5-04」行核对成立）

## 允许写入

- 首批 spike 实现落点：`mymuduo/Buffer.h` / `mymuduo/Buffer.cc`（池化单变量；精确改动点于 RED 前读码后在卡内圈定）。
- `CMakeLists.txt`（仅新增 `ENABLE_BUFFER_POOL` option 构建开关，默认 OFF；不触碰既有 target 结构）。
- `tests/unit/` Buffer 池化契约测试（新增 `BufferPoolContractTest*`；`tests/CMakeLists.txt` 仅新增目标注册）。
- `docs/reports/performance/hotspot-ledger.md`（台账）。
- `docs/tasks/P5-04.md`（本卡）。
- `docs/process/implementation-sop.md`（仅头部活动任务两行 L5/L7）。
- `docs/process/implementation-progress.md`（仅 P5-04 行）。

未列路径禁止。禁止：

- **flag OFF 主线等价**：默认构建零新代码路径，EPollPoller/TcpConnection/OutputEncoder/P1R-03 发送预算逻辑零修改；Buffer 公开接口语义（retrieve/peek/retrieveAll/readFd/internalBuffer 等）零变化。
- io_uring Poller 已登记跳过——不得在本环境强行实现 io_uring 路径（WSL2 EINVAL，计划 §9.6「不可用直接登记跳过」）。
- sendmmsg/recvmmsg、MSG_ZEROCOPY 候选在进入条件证据补齐前不得开工（§9.9 停止规则：没有 profile 证据就开始 spike 实现即停止）。
- 性能门槛外的任何优化变量（单变量纪律）；bench 方法学零更改（P5-02 冻结）。
- 不 stage：本卡不执行 `git add/commit`（由提交者负责）。profile/bench 产物只落 `/mnt/d/muduo-chat-build/`（构建树外），永不进仓库。

## Spike 进入条件核查表（RED 前冻结，2026-08-25）

| 候选 | 进入条件（计划 §9.6 全文） | 证据核查 | 判定 |
|---|---|---|---|
| io_uring Poller 替换 EPollPoller | 先做内核可用性 spike（WSL2 内核版本/config 核实），不可用直接登记跳过 | 编排者实测（2026-08-25）：内核 **6.6.87.2-microsoft-standard-WSL2**；liburing 未安装（sudo 需密码交互无法安装）；原始 syscall `io_uring_setup(entries=4, flags=0)` → **errno=22 EINVAL**（WSL2 环境 io_uring 对本实现不可用）。探测程序留存 `/mnt/d/muduo-chat-build/p5-04-spike/iouring_probe.c`（+ 已编译 `iouring_probe`，本 worker 已复核文件存在） | **跳过登记**。复评条件 = 环境变化（WSL 内核升级启用 io_uring / 裸 Linux 部署），届时重开探测再评估 |
| sendmmsg/recvmmsg 批量收发 | syscall 占比为热点时才开工 | P5-02 gprof 为 CPU flat profile（16 采样/0.16s 定性，M-4 样本厚度声明），**无 syscall 占比数据**；strace -cf 未采集；DB I/O wait 对 gprof 不可见（p5-baseline.md 登记）。echo ~21.4k msg/s 无 syscall 瓶颈信号 | **证据不足，不开工**（无 profile 显示 syscall 占比为主要瓶颈；如需证据先补 strace -cf 计数类 profile 再复核本表） |
| MSG_ZEROCOPY 大 payload 路径 | 拷贝占比显著时才开工 | gprof 定性第一热点为 `vector<char>` emplace_back（含序列化往返拷贝），但样本厚度声明明确 1-2 样本粒度仅供定性排序，**拷贝占比无独立数据**；大 payload 场景 slow-consumer 为字节一致性场景（吞吐非主指标） | **弱证据，不开工**（留观察；若后续出现独立拷贝占比数据且显著，重开本表复核） |
| Buffer 内存池化（P5-03 移交） | 分配次数/RSS 增长显著时才开工 | `vector<char>` emplace_back **317,263 次**（gprof 调用计数，flat profile 第一热点——四候选中实证相对最强）；RSS 侧 ChatServer ~16.4MB 5 轮稳定无增长趋势（收益预期在分配 churn/CPU，**不主张内存占用收益**）；P5-03B（3fb46bb）已消费回复序列化拷贝切片，剩余 churn 落在 mymuduo Buffer 字节操作路径。**注意**：317,263 为 350d4e2 profile 树历史值，P5-03B 之后 HEAD 的分配计数须 RED 前重测（见 RED 设计观察基线） | **相对最强实证，选为首批 spike**（进入依据 = 分配次数维度；RSS 无增长如实登记） |

## Spike 协议冻结（计划 §9.6）

- 每个候选独立 feature flag 构建开关；**flag 名冻结（§9.8）：CMake option `ENABLE_BUFFER_POOL`，默认 OFF**。
- flag OFF 时与主线行为等价、默认配置零新代码路径；fallback adapter 常驻（EPollPoller 与原 Buffer 存储路径不动）。
- 同一 contract/process 正确性套件对 flag ON/OFF 双跑。
- sanitizer 三树：Debug / ASan+UBSan / TSan（ON 态至少一轮全量）。
- benchmark 子集复测：同 P5-02 冻结方法学（echo + reliable-direct 子集，≥5 轮 + 95% CI 对照）。
- 无收益即删除代码、结论留台账（删除不留死代码）；转正进默认路径必须先立 ADR 并附 P5-02 方法学下的对比数据。
- 一个 spike 一个提交序列（`experiment(io): <name>`，不合入则只在台账留结论）。

## io_uring spike 结论登记

判定来源 = 编排者实测（2026-08-25），本 worker 开卡复核探测产物存在性与内核版本一致：

- 内核 `6.6.87.2-microsoft-standard-WSL2`；liburing 未安装（apt 安装需 sudo 密码交互，无授权不安装——§9.0 环境授权清单约束）。
- 绕过 liburing 的原始 syscall 探测：`syscall(__NR_io_uring_setup, 4, &params)` 返回 -1，errno=22 EINVAL → WSL2 内核对 io_uring 不可用（非 liburing 缺失问题）。
- 登记：io_uring Poller spike 按 §9.6「不可用直接登记跳过」收口；本环境不再投入实现；环境变化可复评（复评动作 = 重跑 `/mnt/d/muduo-chat-build/p5-04-spike/iouring_probe.c`）。

## RED 设计（首批 spike：Buffer 内存池化）

- **契约测试（flag ON/OFF 双跑）**：新增 `tests/unit/BufferPoolContractTest*`——池化存储与主线 `std::vector<char>` 存储行为契约等价（append/retrieve/peek/retrieveAll/readableBytes/internalBuffer 语义、借还幂等、边界读写）；OFF 构建下同一契约走原路径全绿（等价性固定），ON 构建下走池化路径全绿。RED 形式 = 引用尚不存在的池化入口编译失败（合法 RED，沿 P4/P5 惯例）。
- **分配计数观察基线（RED 前记录）**：
  - 历史 anchor：gprof `vector<char>::emplace_back` 317,263 次（350d4e2 profile 树，p5-baseline.md；P5-03B 前的历史值，只作方向依据）。
  - 当前基线：HEAD f4ae57f 重测分配计数口径（gprof 变体或插桩计数变体，沿 P5-02 fallback 工具链）+ bench 子集 before 基线 ≥5 轮原始 JSON（`/mnt/d/muduo-chat-build/p5-04-before-*.json`，构建树外）。
- **性能门槛（RED 前冻结）**：
  - 主门槛 = **echo 场景 msg/s 提升 ≥15% 且 95% CI 不重叠**（Buffer churn 为网络层 CPU 成本，echo 是最直接受益场景；分母 = P5-02 echo CV 上界 r1b 0.046，Δ 可观测 ≈ 0.046×21368 ≈ 983 msg/s，门槛 ≈3200 msg/s 远超噪声上界）。
  - 护栏 = reliable-direct p99 回退 ≤5%、正确性零回退（正确性矩阵 + fault 套件全程全绿）、slow-consumer 字节一致性语义不变。
  - 判定口径 = P5-02 冻结方法学（同负载同方法学前后各 ≥5 轮，均值 + 95% CI(t) + CV；前均值落在后均值 CI 外）。未达主门槛 → 无收益处理：删除代码、结论入台账（§9.6）。
  - 若 HEAD 重测分配计数较 317,263 显著下降（P5-03B 消费所致）导致收益预期不足，允许在 RED 前如实降级为「评估后不开工」并登记台账——不预设结果。

## 阶段 1 基线与 RED 登记（luna_worker，2026-08-25）

### 正确性基线（flag OFF = 当前主线）

- HEAD f4ae57f 全新 configure 树 `/mnt/d/muduo-chat-build/p5-04-debug`（Debug，ENABLE_TESTS=ON）：ctest **497/497 全绿**（Total 698.5s），CTEST_RC=0。log：`/mnt/d/muduo-chat-build/p5-04-before-fullctest.log`（configure/build：同目录 `p5-04-before-configure.log` / `p5-04-before-build.log`）。
- Kafka/Redis/MySQL 存活核对成立：真实集成用例通过（KafkaOutboxFixture 13 用例、RedisPresenceContractTest、ClusterChaosProcess、ReliableMessageFaultProcess 等）；bench preflight 再次确认 MySQL 3306 / Redis 6379 / Kafka 9092。

### bench before（P5-02 冻结方法学，n=6）

产物：`/mnt/d/muduo-chat-build/p5-04-before.json` + `p5-04-before.log`；workload 口径 = chat-bench echo（4 conn × 200 msg × 64 B）+ load.py reliable-direct（16 conn × 2000 ms）。

- **echo**：msg/s mean=**17788.42**，95% CI=[17453.27, 18123.57]，CV=**0.0180**（低于分母上界 r1b 0.046）；p50 mean=0.152 ms，p95 mean=0.493 ms，p99 mean=0.660 ms；RSS 3840 KB 恒定。
- **reliable-direct**：msg/s mean=22.17（rep3 单轮离群 12.0 → CV=0.225）；p50 mean=1045.79 ms CI=[482.87, 1608.71]；p95 mean=1216.95 ms CI=[647.96, 1785.95]；**p99 mean=1235.20 ms CI=[661.37, 1809.04]**（≤5% 回退护栏基准）；RSS mean≈16341 KB 稳定无增长。
- 本组数字即 spike 的 before 对照（after 复测须同口径 ≥5 轮 + `tests/scripts/p5_02_stats.py` CI 对照）。

### 分配计数观察基线

- 历史 anchor（只作方向依据）：gprof `vector<char>::emplace_back` **317,263 次** @350d4e2 profile 树（reliable-direct n=3，p5-baseline.md）。
- **HEAD f4ae57f 重测 = 92,406 次**（复刻 P5-02 fallback 工具链同口径：`-pg` Debug 树 `/mnt/d/muduo-chat-build/p5-04-gprof`（ENABLE_TESTS=OFF），run.py reliable-direct n=3）。较 anchor **−70.9%**——P5-03B 消费回复序列化拷贝切片的效应在分配计数上如实显现。
- 产物：`/mnt/d/muduo-chat-build/` 下 `p5-04-gprof-configure.log` / `-build.log` / `-run.log` / `p5-04-gprof-run.json` / `p5-04-gprof-profile.txt`（树内 gmon.out）。
- 如实登记：进入条件所依赖的分配次数维度实证已显著下降；是否触发本卡 RED 设计「评估后不开工」降级条款由编排者按 §9.6 判定，本阶段不预设结果（RED 已按计划落地）。

### RED 契约测试（合法编译失败）

- 新增 `tests/unit/BufferPoolContractTest.cpp`，沿 BufferTest 风格，3 用例：
  1. **PoolOffIdenticalToBaseline**——flag OFF 时与 baseline 完全等价（append/retrieveAll/readableBytes/writableBytes/prependableBytes 语义），且池化观察面必须报 `enabled=false` 零计数；
  2. **PoolOnPreservesSemantics**——公开语义两构建同跑不变（边界读写、partial retrieve、retrieveAll 复位、超初始增长、ensureWritableBytes、readFd 内部区路径）；ON 态附加 `enabled=true`；
  3. **PoolReuseObservation**——ON 态重复构造/析构后 free-list 命中计数增长（复用可观察）；OFF 态 `enabled=false` 且 hits/releases 恒零（不得虚报池化）。
- 观察面设计（GREEN 待实现）：嵌套类型 `Buffer::BufferPoolStats{enabled, acquires, hits, releases}` + `static Buffer::poolStats()`；flag 编译期注入宏 `MUDUO_ENABLE_BUFFER_POOL`（CMake option ENABLE_BUFFER_POOL，名冻结 §9.8）；测试用 `#if defined(MUDUO_ENABLE_BUFFER_POOL)` 分支保证同一文件 OFF/ON 双构建可编译可通过。
- `tests/CMakeLists.txt` 仅新增目标注册三处：target 块（含注释）、foreach ITEMS 追加、`discover_gtest(BufferPoolContractTest)`。
- **RED 验证**：隔离审查树 `/tmp/muduo-chat-build/p5-04-red`（Debug，ENABLE_TESTS=ON），`cmake --build --target BufferPoolContractTest` → **exit 2（合法 RED）**；首个失败点 `tests/unit/BufferPoolContractTest.cpp:35:19: error: 'BufferPoolStats' in 'class Buffer' does not name a type`（case 1 OFF 分支），case 3 同型失败于 :114。log：`/mnt/d/muduo-chat-build/p5-04-red-build.log`（configure：`p5-04-red-configure.log`）。
- ON 态编译与 ON/OFF 双跑全绿留待 GREEN 阶段（flag 注入 + 观察面实现后）验证。

## 阶段 1 复评与收口决定（luna_worker，2026-08-25）

**判定：CLOSED——四候选全部有据跳过，spike 收口不实现（无实现提交，无 spike 代码进入仓库）。**

### 复评证据（阶段 1 实测，见上节）

- **正确性基线**：HEAD f4ae57f 全新 configure 树全量 ctest **497/497 全绿**（零回退基线成立）。
- **bench before**：echo msg/s mean=**17788.42**（CI [17453.27, 18123.57]，CV=**0.0180** ≤ 分母上界 0.046）；reliable-direct p99 mean=1235.20 ms（CI [661.37, 1809.04]，护栏基准）。
- **分配计数重测（决定性证据）**：HEAD f4ae57f `vector<char>` emplace_back = **92,406 次**，较 @350d4e2 anchor 317,263 次 **-70.9%**（P5-03B 序列化拷贝消除已消费回复序列化拷贝切片的效应在分配计数上如实显现）。进入条件所依赖的「分配次数为主要瓶颈」实证不再成立。

### 四候选逐项收口结论

| 候选 | 结论 | 依据 |
|---|---|---|
| io_uring Poller | 跳过（环境不可用） | WSL2 内核 6.6.87.2 原始 syscall `io_uring_setup` → errno=22 EINVAL；liburing 未安装且无授权安装。探测程序留存 `/mnt/d/muduo-chat-build/p5-04-spike/iouring_probe.c` |
| sendmmsg/recvmmsg | 跳过（证据不足） | 无 syscall 占比 profile 数据（gprof 为 CPU flat profile、DB I/O wait 不可见），不满足 §9.6 进入条件 |
| MSG_ZEROCOPY | 跳过（弱证据） | 拷贝占比无独立数据（16 采样定性排序不作占比解读）；大 payload 场景非吞吐主指标 |
| Buffer 内存池化（首批 spike） | **进入条件失效，不开工实现** | 上节分配计数 -70.9%：分配 churn 已非主要瓶颈；RED 设计预置的降级条款（「若 HEAD 重测显著下降导致收益预期不足，允许 RED 前如实降级」）如实触发 |

### 处置

- 按 §9.6「无收益即删除代码、结论留台账」：spike 不落地任何实现代码（RED 阶段本就未改 Buffer/CMakeLists 实现）；`tests/unit/BufferPoolContractTest.cpp` **保留为候选规格**（头部注明非构建目标），`tests/CMakeLists.txt` 构建注册移除（沿 P5-03A/C 先例三处：target 块 / foreach ITEMS / discover_gtest），重新 configure + ctest -N 回到 497。
- 台账登记：`docs/reports/performance/hotspot-ledger.md`「P5-04 收口登记」节。
- §9.9 停止规则全程零触发（未在有据前开工任何 spike 实现）。

### 复评条件

满足其一即可重开本卡候选复核：

1. **环境变化**：WSL 内核升级启用 io_uring / 迁移裸 Linux 部署（复评 io_uring，动作 = 重跑探测程序）。
2. **后续 profile 显示分配 churn 回升为主要瓶颈**（如新增热路径引入大量 Buffer 字节拷贝/扩容；复评 Buffer 池化与 sendmmsg/recvmmsg、MSG_ZEROCOPY 的占比类证据，先补 strace -cf / 拷贝占比独立数据再复核进入条件核查表）。

## 最小实现（单变量）

- `CMakeLists.txt` 新增 `option(ENABLE_BUFFER_POOL "P5-04 buffer pooling experiment" OFF)`；OFF 时编译产物与主线逐字节等价路径（零新代码进默认二进制的行为面）。
- Buffer 池化单变量：`mymuduo/Buffer` 内部存储从每次扩容堆分配改为预分配块复用（free-list chunk 池）；只动内部存储策略，不改公开接口签名/语义、不动 TcpConnection/OutputEncoder/SendOutcome 预算路径、不引入配置项（flag 即唯一开关）。
- 单变量纪律：一次只验证池化一个变量；sendmmsg/recvmmsg、MSG_ZEROCOPY 不混入。

## 验证

- flag 双跑：`ctest --test-dir <tree> -R 'Buffer|MultiReactor|Backpressure' --output-on-failure` ON/OFF 各全绿；ON 态 Debug 全量 + ASan+UBSan + TSan 三树（TSan 沿 setarch x86_64 -R 口径）。
- OFF 态构建等价性：默认 configure（不加 flag）全量 ctest 与主线一致（基线 497/497 @ f4ae57f，以 RED 前复测为准）。
- bench 子集：`tools/bench/run.py --scenarios echo,reliable-direct` 前后各 ≥5 轮 + `tests/scripts/p5_02_stats.py` CI 对照。
- `git diff --check` exit 0；diff 只含允许范围。

## 完成定义

- 默认构建（flag OFF）与主线行为等价且有测试固定（契约双跑证据）。
- 首批 spike 有「继续/删除」结论与数据支撑：达标 → 台账登记 + ADR 提案路径（转正另走 ADR + 独立 feat 提交）；未达标 → 删除代码、台账留结论、不留死代码。
- io_uring 跳过登记在案（本卡 + 台账）；sendmmsg/recvmmsg、MSG_ZEROCOPY 维持「证据不足不开工」登记，证据补齐须先修订本表。
- §9.9 停止规则全程零触发。

## Commit

一个 spike 一个提交序列：`experiment(io): buffer-pooling`（或按实际落点调整消息；不合入则只在台账留结论、无提交）。由提交者执行，本卡 worker 不 stage。

## 开卡声明（luna_worker，2026-08-25）

本卡开卡 + io_uring 跳过登记 + 进入条件核查表冻结 + 首批 spike 选定，未做任何实现。已创建：`docs/tasks/P5-04.md`（本卡）。已更新：`docs/reports/performance/hotspot-ledger.md`（P5-04 开卡登记节，核对 P5-03 循环结论「移交 P5-04」行成立）、`docs/process/implementation-sop.md`（头部活动任务两行）、`docs/process/implementation-progress.md`（P5-04 行）。已复核：HEAD=f4ae57f（P5-03 收口提交）、io_uring 探测产物 `/mnt/d/muduo-chat-build/p5-04-spike/iouring_probe.c`（+.c 编译产物）存在、内核 6.6.87.2-microsoft-standard-WSL2 一致、liburing 未安装、mymuduo Buffer 落点为 `mymuduo/Buffer.h|.cc`、既有契约 `tests/unit/BufferTest.cpp`。未 `git add/commit`，未改任何实现代码。
