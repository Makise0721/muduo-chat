# P1R-09 P1 独立完成性验收

状态：VERIFIED
基线：main @ 51ade18（P1R-08 后）
依赖：P1R-00..P1R-08 全部 VERIFIED
允许写入：/docs/tasks/P1R-09.md、/docs/IMPLEMENTATION_PROGRESS.md、/docs/IMPLEMENTATION_SOP.md
（活动行/状态）、/docs/EVOLUTION_PLAN.md（状态行如需）
禁止修改：生产源码、测试、CMake（本任务零代码改动）。

## 问题与证据

P1_COMPLETION_REVIEW_00db852 的 S-01..S-07 / F-01..F-06 / A-01..A-03 逐项回查
（整改证据映射）：

| 审查项 | 整改任务 | 证据 |
|---|---|---|
| S-01 信号 handler 可能阻塞 | P1R-05 | self-pipe 两端 NONBLOCK|CLOEXEC；进程风暴 CTest（DRAINED 恰一次） |
| S-02 P1-07 缺 RED | P1R-00 后各任务卡 | P1R-01..08 卡均含 RED 命令/实际证据 |
| S-03 越界写入 | P1R-00 | 允许写入集合逐卡核对（P1R 各卡显式列出） |
| S-04 状态矛盾 | P1R-00 | 任务卡状态唯一（P1R-09 为唯一 IN_PROGRESS） |
| S-05 diff gate | P1R-00 | git diff --check 全仓库 exit 0 |
| S-06 Middle Man | P1R-00 记录 | ChatServer 透传方法在 P2 建立 ChatApplication 边界时收敛 |
| S-07 CLAUDE.md 过期 | P1R-00 | out-of-source + CTest/Sanitizer 闸门已更新 |
| F-01 发送准入无界 | P1R-03 | SendOutcome 双轴语义 + CAS 预算 + 跨线程 WouldBlock 测试 |
| F-02 跨 EventLoop 强关 | P1R-04 | forceClose queueInLoop 调度 + threadNum=2 测试 + TSan |
| F-03 过载降级 | P1R-06 | accept rate + EMFILE idle-fd + fd-exhaustion 进程 CTest |
| F-04 codec 上限 | P1R-02 | EncodeResult + 16MiB 校验 + 失败不改 output |
| F-05 timer 相位 | P1R-07 | planned phase 保持 + 跳周期 + 长回调测试 |
| F-06 日志未完成 | P1R-08 | LogEvent 结构化 + 批量 sink + bench 报告 |
| A-01 TimerId 空指针 | P1R-07 | cancel(TimerId{}) 幂等 no-op 测试 |
| A-02 协议错误 half-close | P1R-02 | force-close + 进程 EOF smoke |
| A-03 测试未链接生产库 | P1R-01 | compile_commands 单条目断言 + 链接 mymuduo target |

## 非目标

- 不实现新功能；不重跑已完成任务的红灯；不修改任何代码。

## 验证矩阵（2026-08-10 实测）

1. 全新 4 树：debug（103/103 → 修复后 105/105）、asan 105/105、tsan 105/105 ×2
   无 WARNING、Release tests-off 构建成功（4 二进制）；
2. 关键并发套件 Backpressure|TcpServer|TimerQueue|Logger 37 测试 ×5 轮 ×4 组稳定；
3. 进程矩阵：SIGINT/SIGTERM 风暴 CTest（双场景：挂连接 5s 硬截止 DRAIN_TIMEOUT +
   零连接风暴 DRAINED 恰一次）3× 稳定；fd-exhaustion CTest（CONNECTED=123 → RECOVERED=5）；
   v1/v2 注册/登录等价 smoke；reconnect storm 30× + 稳定登录；LOG_FATAL 进程证据
   （[FATAL] 输出 + exit(-1)）；
4. 文档：git diff --check exit 0；任务卡状态唯一；P1R 卡均含 RED/实际证据；
5. 双轴最终审查（子代理）：Spec 轴 P1R-01..08 完成定义全 met（16MiB 边界测试、
   OverloadPolicy 接口偏差已在卡内记录）；Standards 轴 1 个 P1 级 claim
   （cancel 已触发 TimerId UAF）经 shared_ptr 计数分析判定为误报，并以
   CancelFiredTimerRepeatedlyIsSafe（500 轮 × ASan）强化证明；空消息预算泄漏
   （release 修复）+ 硬截止 CTest 场景补齐闭环。

## 完成定义

- 两轴重审无 P0/P1 未决项：**达成**（P1R-09 修复闭环后 105/105 ×TSan 两轮）；
- P1 整体标记 VERIFIED（REMEDIATION 解除）；允许 P2-00 开始。

## Commit

实际提交：`docs: verify P1 remediation gates`
