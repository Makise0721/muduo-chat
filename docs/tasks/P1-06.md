# P1-06 异步结构化日志

状态：PLANNED
基线：main @ 8c3edf9（P1-05 提交后）
依赖：P1-05 VERIFIED
允许写入：/mymuduo/Logger.h/.cc、/tests/unit/LoggerTest.cpp、/tests/CMakeLists.txt、
/docs/tasks/P1-06.md、/docs/IMPLEMENTATION_PROGRESS.md、/docs/IMPLEMENTATION_SOP.md（仅活动任务状态行）
禁止修改：其余生产源码；LOG_* 宏调用点（兼容）。

## 问题与证据

EVOLUTION_PLAN §2.1 在案：#4 Logger 全局共享可变状态（宏每次 setLogLevel 写 logLevel_ +
同步写 std::cout + Timestamp::toString 共享）——TSan 全量失败 3 项的 2 项归因于此；
同步输出进热路径（每事件 LOG_INFO）。

## Interface / 可观察行为（EVOLUTION_PLAN §9 P1-06）

- LOG_* 宏调用点不变；一次调用携带级别；
- 有界异步队列（默认 4096）+ 独立 logging 线程；热路径不做磁盘 I/O；
- 队列满：丢弃新日志，`droppedCount()` 可观察；
- `Logger::setLogLevel` 级别过滤（低于级别零开销跳过）；
- `Logger::flush()`：阻塞至队列排空（测试/退出用）；LOG_FATAL 强制 flush 后 exit；
- 日志线程单线程写输出 → 消除 setLogLevel/cout 竞态（TSan 在案）。

## 非目标

- 不做文件滚动/通用日志生态（EVOLUTION_PLAN："不先实现"）；
- 不做结构化 JSON 日志（"结构化"指携带级别/上下文字段，文本输出即可）；
- fatal 进程内测试 N/A（exit 语义，单独进程验证）。

## RED

命令：

```powershell
wsl.exe -d Ubuntu -- cmake --build /tmp/muduo-chat-build/p0-02b-red --parallel
```

预期失败：LoggerTest 引用 flush/droppedCount 不存在 → 编译错误。

实际证据：待记录。

## 最小实现

- Logger：atomic logLevel_ + mutex/condvar + 有界 deque + logging 线程 + dropped 计数 +
  flush()/droppedCount()/注入输出流（测试 seam）；
- LOG_* 宏：级别检查（跳过）→ 栈 buffer snprintf → log(level, buf) 入队；
- LOG_FATAL：入队 + flushAll + exit(-1)；
- 析构：join logging 线程（退出前排空）。

## 验证

- 聚焦：`ctest -R LoggerTest`；
- 全量：Debug + ASan（LSan 无泄漏——logging 线程 join）；
- TSan：Logger 专项（setarch -R）无竞态（在案 #4 关闭）；
- 进程级：ChatServer smoke 日志仍工作（双协议注册/登录）。

## 完成定义

- 级别过滤/队列满丢弃计数/多线程无串扰全部断言；
- 热路径无同步写（实现结构保证 + 记录）；
- Debug/ASan/TSan(Logger) 通过；
- 既有 69 测试全绿。

## Commit

建议：`feat(logging): add bounded async logger`（获授权后执行）
