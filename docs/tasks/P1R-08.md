# P1R-08 完成最小结构化异步日志模块

状态：VERIFIED
基线：main @ 62f36be（P1R-07 后）
依赖：P1R-07 VERIFIED；审查报告 F-06
允许写入：/mymuduo/Logger.h/.cc、/tools/logger-bench/main.cpp、/tools/CMakeLists.txt、
/tests/unit/LoggerTest.cpp、/docs/tasks/P1R-08.md、/docs/IMPLEMENTATION_PROGRESS.md、
/docs/IMPLEMENTATION_SOP.md（活动行）、/docs/performance-reports/
禁止修改：其余生产源码；LOG_* 宏调用点；协议。

## 问题与证据

审查报告 F-06：队列只保存 (level, string)，输出无结构化 context；消费端逐条写且
flush 型输出（std::endl），无批量/double-buffer；无同步 vs 异步对比 benchmark。
Logger.cc:129 用 `<< std::endl`（逐条 flush）；run() 单条取队列（:98-100）。

## Interface / 可观察行为

```cpp
struct LogEvent {
    Timestamp timestamp;      // 入队前固定
    int level;
    std::thread::id threadId; // 入队前固定
    std::string component;
    std::string eventName;
    std::string message;
};
void log(int level, const std::string &component, const std::string &eventName,
         const std::string &message);
using SinkCallback = std::function<void(const std::vector<LogEvent> &)>;
void setSinkCallback(const SinkCallback &cb);  // test recorder adapter
```

- LOG_* 宏调用点零改动（宏内部改调 4 参版本，component 空、eventName=宏名）；
- 单消费者按 batch（≤64）取队列、批量写（移除逐条 flush：endl → '\n'）；
- 默认输出格式不变（前缀 + timestamp + message）；test recorder 经 setSinkCallback
  断言结构化字段；
- 队列满 drop policy、FATAL 必达、flush 语义不变。

## 非目标

- 不做文件滚动/轮转、不做 double-buffer 无锁优化、不预设性能提升比例。

## RED

命令：`wsl.exe -d Ubuntu -- bash -c "ctest --test-dir ~/muduo-chat-build/debug -R Logger"`
预期失败：StructuredFieldsReachableByRecorder——当前 log() 无 component/eventName，
recorder 接口不存在。
实际证据：编译失败（无 LogEvent/SinkCallback）。

## 最小实现

1. Logger.h：LogEvent 值对象；log 4 参重载（保留 2 参兼容？——宏改 4 参即可，删 2 参）；
   setSinkCallback；run() 批量取 + 批量写（sinkCallback 或 ostream，'\\n' 不 flush）；
2. LOG_* 宏：`logger.log(LEVEL, "", "LOG_XX", buf)`；
3. LoggerTest：recorder 断言字段（level/component/eventName/threadId/timestamp）、
   批量顺序、多线程 threadId 不串扰；既有测试适配签名；
4. tools/logger-bench：同步（直接写 /dev/null）vs 异步（Logger→/dev/null）吞吐 +
   p50/p95/p99 调用时延 + drop count；CMake 注册；
5. 跑 bench 生成 docs/performance-reports/<commit>.md（原始参数 + 结果，不预设比例）。

## 验证

- `ctest -R Logger` 9/9 ×3×5 轮稳定；全量 Debug/ASan/TSan 103/103 ×TSan 两轮无 WARNING；
- 新增 3 测试：StructuredFieldsReachableByRecorder（level/component/eventName/
  threadId/timestamp 断言）、BatchPreservesOrder（200 条顺序）、
  ThreadIdDoesNotInterleave（4 线程 component→threadId 映射一致）；
- 既有测试适配 log 4 参签名；FATAL 必达/flush/级别过滤/多线程完整性全部保留；
- logger-bench 结果与解读见 docs/performance-reports/97c0311.md（sync 38M vs async
  1.4M msg/s、async p50≈250ns、dropped 可查、无未解释回退）；
- `git diff --check` exit 0。

## 调试记录

1. LogEvent 结构体最初未写入头文件（一次 edit 静默未命中目标）→ 编译错——补齐。
2. bench 初版 duration 数组 p95=1ns 异常——vector<steady_clock::duration> 条目
   默认 0 与真实值混排；改用 long long ns 数组显式转换后 p50/p95/p99 合理。
3. 批量取队列上限 64（batch 写减少锁外竞争）；std::endl → '\n' 移除逐条 flush
   （F-06 项）。

## 完成定义

- 结构化字段可被 test recorder 断言；FATAL 不丢；正常退出不丢已接收事件；
- 异步模式无未解释性能回退（报告含同步/异步对比）。

## Commit

建议：`feat(log): batch structured events asynchronously`（获授权后执行）
