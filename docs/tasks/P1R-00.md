# P1R-00 恢复事实、任务状态与审计边界

状态：VERIFIED
基线：main @ 00db852
依赖：P1_COMPLETION_REVIEW_00db852（外部完成性审查，判 P1 REOPEN）
允许写入：/docs/tasks/P1R-00.md、/docs/tasks/P1-06A.md、/docs/IMPLEMENTATION_PROGRESS.md、
/docs/IMPLEMENTATION_SOP.md、/docs/EVOLUTION_PLAN.md（仅行尾空白清理）、/CLAUDE.md（构建/测试章节）
禁止修改：生产源码、测试源码、CMake、任何 `.cc/.h/.cpp/.hpp`、任务卡中已提交的历史 RED 证据文本（P1-06A 仅状态行）。

## 问题与证据

P1_COMPLETION_REVIEW_00db852 判 P1 完成性不通过（S-04/S-05/S-07 为文档/流程问题），
要求恢复状态唯一性与审计边界。基线证据（2026-08-10 采集）：

- S-04：`docs/tasks/P1-06A.md:3 状态：IN_PROGRESS`，而 `IMPLEMENTATION_PROGRESS.md` 已记
  VERIFIED（提交 6f29a1c）——状态互相矛盾；
- S-05：`git diff --check 1b94d43...HEAD` 报 EVOLUTION_PLAN.md:3,4 与 IMPLEMENTATION_SOP.md:3,4,5
  trailing whitespace（Markdown 强制换行双空格）——仓库 diff gate 失败；
- S-07：`CLAUDE.md` "Standard Build" 仍描述源码树内 `mkdir build && cmake ..`，未反映
  out-of-source 约束与 CTest/Sanitizer 闸门；
- SOP 活动行（:6）仍写"P1-07 VERIFIED…下一任务 P2-01"；PROGRESS.md:37 P1-07 行写"待提交"
  （实际已提交 00db852）——与当前 HEAD 不符；
- SOP §4.2/§7 构建命令仍指向 `/tmp/muduo-chat-build`（2026-08-09 WSL 实例回收 /tmp 后已迁移
  `~/muduo-chat-build`）；TSan 运行缺 `setarch x86_64 -R` 前缀与编辑后 `sleep 2` 提示。

## Interface / 可观察行为

进度表、SOP、任务卡对当前 HEAD 给出同一个状态：P1 整体为 `REMEDIATION`（链接审查报告与
POST_P1_IMPLEMENTATION_PLAN），P1R-00 为唯一 `IN_PROGRESS`；历史偏差仍可从 Git 提交定位。

## 非目标

- 不补造历史 RED 失败证据（POST_P1 计划明文禁止）；
- 不重写已提交的任务卡历史内容；
- 不修改生产/测试代码；
- 不执行任何构建（本任务无代码改动）。

## RED

命令：`git diff --check 1b94d43...HEAD`；`rg -n 'IN_PROGRESS|待提交|下一任务' docs`
预期失败：diff --check exit != 0（5 处 trailing whitespace）；P1-06A IN_PROGRESS 与进度表矛盾；
SOP/PROGRESS 行与 HEAD 不符。
实际证据：见"问题与证据"节（exit 2，5 行 whitespace；状态矛盾与过时行逐条列出）。

## 最小实现

1. P1-06A 状态行 → VERIFIED（附注：提交 6f29a1c；IN_PROGRESS 残留系提交时未更新状态行）；
2. SOP 顶部：状态行更新为 P1 `REMEDIATION`、基线 00db852、上位设计增加
   POST_P1_IMPLEMENTATION_PLAN.md 链接、活动任务 P1R-00；
   §4.2 构建目录 → `~/muduo-chat-build/{debug,asan,tsan,release}`（附 WSL /tmp 回收注记）；
   §7 各节命令同步路径，TSan 运行加 `setarch x86_64 -R`，命令注记加编辑后 `sleep 2`；
   §9 开头加注"P0 已全部完成（63a76ed..703ef9f），本节为历史基线"；移除行尾双空格；
3. EVOLUTION_PLAN.md：仅移除 :3,:4 行尾双空格；
4. IMPLEMENTATION_PROGRESS.md：P1-07 行"待提交"→ 已提交 00db852；顶部加 P1 REMEDIATION
   说明行（链接审查报告与 POST_P1 计划）；
5. CLAUDE.md：Standard Build 章节改为 out-of-source + CTest/Sanitizer 闸门 + 构建目录/setarch/sleep2
   实践（其余章节不动）；
6. 本任务卡（P1R-00）补齐验证证据后置 VERIFIED。

## 验证

- `git diff --check -- docs CLAUDE.md`：exit 0（DIFF_CHECK_OK，S-05 关闭）；
- 状态唯一性：`rg -n '状态：' docs/tasks docs/IMPLEMENTATION_PROGRESS.md` —— P1R-00 为唯一
  IN_PROGRESS（完成后 VERIFIED）；P1-06A VERIFIED（附注修正）；P0/P1 其余任务全 VERIFIED；
  无遗留"待提交"/"下一任务 P2-01"状态行（仅历史文档引用）；
- `git diff --stat`：仅 5 个文档文件（CLAUDE.md、EVOLUTION_PLAN.md、IMPLEMENTATION_PROGRESS.md、
  IMPLEMENTATION_SOP.md、P1-06A.md）+ 新增 P1R-00.md；零代码改动；
- P1 整体 REMEDIATION 状态在 SOP 活动行 + PROGRESS blockquote 双处声明，链接审查报告与整改计划。

## 完成定义

- 只有 P1R-00 为 IN_PROGRESS/VERIFIED；所有历史偏差可从 Git 与审查报告定位；
- diff 无空白错误；CLAUDE.md 构建说明与真实环境一致。

## Commit

建议：`docs: reopen P1 after completion review`（获授权后执行）
