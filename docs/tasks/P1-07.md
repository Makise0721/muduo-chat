# P1-07 对抗审查修复批次

状态：VERIFIED（五轮双轴对抗审查通过）
基线：main @ 873d7f8（P1 全部 VERIFIED）
依赖：P1 双轴对抗审查报告（2026-08-10）
允许写入：/mymuduo/TimerQueue.cc、/mymuduo/Logger.h/.cc、/mymuduo/TcpConnection.cc、
/mymuduo/Acceptor.h/.cc、/mymuduo/TcpServer.h/.cc、/chatserver/main.cpp、
/tests/unit/BackpressureTest.cpp、/tests/unit/LoggerTest.cpp、/tests/unit/TcpServerTest.cpp、
/docs/tasks/P1-07.md、/docs/IMPLEMENTATION_PROGRESS.md、/docs/IMPLEMENTATION_SOP.md（活动行）
禁止修改：其余生产源码；协议字节格式；业务行为；既有公开 API 签名（新增例外）。

## 修复项（对应审查报告）

1. HARD — TimerQueue `timers_` 无锁读：resetTimerfd 内部加锁；
   addTimer 锁内 insert 后**解锁再调** resetTimerfd（std::mutex 非递归，防死锁）。✅
2. HARD — Logger 级别语义颠倒：枚举重排 DEBUG=0, INFO=1, ERROR=2, FATAL=3
   （宏 gate `logLevel() <= LEVEL` 不变；FATAL 宏无条件）；默认值仍 INFO。✅
3. HARD — LOG_FATAL 可丢：log() 满队列时 FATAL 驱逐队首后入队（其余级别仍 drop）。✅
4. MEDIUM — hardLimit 关闭路径：sendInLoop 达 hardLimit 分支调用 startStallTimer()
   （stallActive_ 防重），使"hard 触发断开"成立；返回值仍 Backpressured。✅
5. MEDIUM — 补断言：BackpressureTest 新增断连后 send==Closed 断言；
   hardLimit 触发 stall 关闭测试（小 hardLimit + stallTimeout 后连接关闭）。✅
6. MEDIUM — Acceptor::acceptErrorCount() getter + TcpServer 透传 + 断言初始 0。✅
7. LOW — beginShutdown 幂等（二次 SIGINT 不再注册重复定时器）。✅
8. 在案记录不改：Flush 步骤（卡未写）、raw-this 捕获、forceCloseAllConnections
   loop-affinity（threadNum=0 安全）。

## RED

P1-07 修复后 Debug/ASan 全量通过 + TSan 全量无 WARNING（连续 2 轮）。✅ 已达成，见"验证"。

## 验证

| 项 | 结果 |
|---|---|
| Debug 全量 | 100% tests passed, 0 tests failed out of 78 |
| ASan 全量 | 100% tests passed, 0 tests failed out of 78 |
| TSan 第 1 轮 | 100% tests passed, 0 tests failed out of 78，无 WARNING |
| TSan 第 2 轮 | 100% tests passed, 0 tests failed out of 78，无 WARNING |
| TSan 定向（Logger\|TimerQueue\|Backpressure） | 100% tests passed, 0 tests failed out of 20 |
| `git diff --check -- mymuduo chatserver tests docs/tasks/P1-07.md` | exit 0（仅 CRLF 换行提示，无空白错误） |

新增测试 5 例（73→78）：`BackpressureTest.SendAfterForceCloseReturnsClosed`、
`BackpressureTest.HardLimitTriggersStallClose`、`TcpServerTest.AcceptErrorCountStaysZero`、
`LoggerTest.DebugLevelShowsInfoAndError`、`LoggerTest.FatalNotDroppedWhenQueueFull`。

## 调试记录（子代理复核 2026-08-10）

工作树修复已就位（未提交），本记录为独立复核证据：

- 逐项核对 7 个修复点源码（TimerQueue 锁内 resetTimerfd / addTimer 锁外调用、
  Logger 枚举重排 + FATAL 驱逐、TcpConnection hardLimit 分支 startStallTimer、
  Acceptor/TcpServer acceptErrorCount 透传、main.cpp gShuttingDown 幂等）——全部与卡一致；
- MUDEBUG 未在任何 CMakeLists 定义，LOG_DEBUG 编译期剔除；
  测试按卡注释采用 INFO/ERROR/FATAL 边界断言（`DebugLevelShowsInfoAndError`）；
- 复核结果与"验证"表完全吻合：78/78 全绿、TSan 两轮无 WARNING、定向 20/20、
  `git diff --check` exit 0（仅 CRLF 提示）。

五轮双轴对抗审查闭环（主代理 2026-08-10 汇总）：

- Round 1：发现 7 项（TimerQueue `timers_` 无锁读 HARD / Logger 级别语义颠倒 HARD /
  LOG_FATAL 可丢 HARD / hardLimit 关闭路径缺失 MEDIUM / Closed 零断言 MEDIUM /
  acceptErrorCount 不可查询 MEDIUM / 双 SIGINT 重复 timer LOW）；
- Round 2 修复 + Round 2 审查：7/7 通过；Standards 剩 capacity-0 驱逐 UB（MEDIUM）、
  FATAL 测试未用容量 1（MEDIUM）、nextSequence_++ 锁外（LOW）等 → 二轮实现子代理修复
  （含 setWriteBufferLimits 守卫放宽为 `resume < pause && resume < hard`——经双轴判定必要且正当）；
- Round 3：Standards 1 HARD（EVOLUTION_PLAN §4.4 约束漂移，未同步文档）+ 1 MEDIUM
  （hard-cap-only 模式无恢复路径——判定为文档化语义）；Spec PASS；
- Round 4：文档措辞过度声明（`checkPause 永不触发`仅在 `2*hardLimit <= pause` 成立）MEDIUM；
  Spec PASS；
- Round 5：PASS——文档与代码逐条算术精确一致，仅测试注释 `<`/`≤` 符号 LOW（已修）。

最终：Debug/ASan 78/78；TSan 78/78 ×2 无 WARNING；五轮闭环通过。

## 完成定义

- TimerQueue 锁内读（TSan 全量干净仍成立）；
- 级别语义：setLogLevel(INFO) 藏 DEBUG 显 INFO+ERROR+FATAL；
  setLogLevel(DEBUG) 全显示；setLogLevel(ERROR) 只 ERROR+FATAL；
- FATAL 在容量 1、队列满时仍被记录；
- hardLimit 达界 + stallTimeout 后连接关闭；
- Closed 状态有断言；acceptErrorCount 可查询。

## Commit

建议：`fix(net): resolve ad-hoc review findings (timer race, log levels, hard-limit close)`（获授权后执行）
