# P5-05 M5 证据包（计划 §9.7；P5 最后一张卡）

状态：**VERIFIED**（2026-08-25 收口，luna_worker；五闸门 fresh 复验由编排者闸门树执行、四轴对抗审查 H/M=0×4、M5 报告已产出 [p5-m5-gates.md](../reports/p5-m5-gates.md)；本状态行随收口提交 `docs: publish M5 evidence package` 一并生效，提交者提交时核对）
基线：main @ f0101e22df79dda6ebefabff33126805ebf3e2f5（`experiment(io): record spike skip evidence`，git rev-parse HEAD 复核一致）
依赖：P5-00..P5-04 全部终态——P5-00/P5-01/P5-02 `VERIFIED`、P5-03 循环收口（A FAILED/REVERTED、B PASS/已提交 main @ 3fb46bb、C FAILED/REVERTED、D 评估不实施）、P5-04 CLOSED（全候选有据跳过）（progress 索引 2026-08-24/25）
计划：`docs/plans/post-p2-implementation-plan.md` §9.7（本卡）+ §9.8/§9.9（冻结值清单与停止规则）+ [架构演进方案](../architecture/evolution-plan.md) §9 M5 行硬条件
先例：[p4-m4-gates.md](../reports/p4-m4-gates.md)（M4 报告形态：正确性/工具/性能三矩阵 + 四轴独立验收 + 数字可追溯表 + Lows 台账）

## 允许写入

- `docs/reports/p5-m5-gates.md`（新建，M5 报告，沿 p4-m4-gates.md 惯例）。
- `docs/tasks/P5-05.md`（本卡）。
- `docs/process/implementation-progress.md`（P5-05 行；收口时 M5 里程碑行同步）。
- `docs/process/implementation-sop.md`（仅头部活动任务两行 L5/L7）。
- `docs/README.md`（导航状态行收口）。

未列路径禁止。**禁止实现代码改动**（验收暴露 H 级缺陷 → 回对应卡流程，本卡不混入修复）。

不 stage：本卡 worker 不执行 `git add/commit`（由提交者负责）；`=1`、`Testing/`、bp_server_dbg.log、bp_server_keep.log、docs/process/p3-handoff-2026-08-13.md 永不 stage。闸门 fresh 运行日志只落构建树外（`/mnt/d/muduo-chat-build/`），永不进仓库。

## 分工与归属登记（开卡冻结）

- **五闸门 fresh 复验由编排者闸门树执行**（Gate A Debug / Gate B ASan+UBSan / Gate C TSan / Gate D Release tests-off + focused 抽认），本卡登记归属，卡内不重复执行、不以 progress 文字替代 fresh 证据。
- 四轴对抗审查（Standards / Spec / Concurrency-Fault / Migration-Security）各轴独立 luna_worker 基于 HEAD f0101e2 + 原始日志审查，互不替代（P4-07 同款）。
- `docs/reports/p5-m5-gates.md` 由闸门树按本卡「Interface / 可观察行为」节冻结的矩阵产出。

## Interface / 可观察行为（§9.7 冻结，不改动）

### 正确性矩阵（五闸门 fresh 复验 + 矩阵抽认）

| 维度 | 要求 |
|---|---|
| 五闸门 fresh | Gate A Debug 全量（无 skip）/ Gate B 显式 ASan+UBSan 零报告 / Gate C 显式 TSan（setarch x86_64 -R）0 WARNING / Gate D Release tests-off 构建 exit 0 无 `*Test` 二进制——全部全新树，沿 P4-07 形态 |
| contract | Telemetry/MetricsSnapshot/PrometheusEndpoint（P5-00）、ReliableMessagingContract/Presence/Redis/Gateway/Outbox 族随全量 fresh |
| migration | SchemaMigration 套件随全量（0001–0003 未在 P5 改动；checksum 幂等复核） |
| direct/group | **P5-03B 已改服务器回复编码路径（ProtocolCodec.cpp encode seam + ChatService 接线）→ 本维度必须 fresh**：ReliableProtocolGolden 字节 pin + ProtocolEncodeContractTest 7 用例 + DomainCharacterization/MultiReactor |
| v1/v2 | ReliableProtocolGoldenTest 字节级 pin + DualProtocol 等价随全量 fresh |
| online/offline | 在线 DeliveryAttempt + 离线 Pending→新 Session claim（DeliveryAckProcess/ReconnectReplayProcess 随全量） |
| retry/reconnect | DeliveryRetry/ReconnectReplay/OutboxCrashRecovery fresh 全绿 |
| worker 矩阵 | 1/2/4/8 行为等价为 P3-11/P3-13 在案 fresh 证据；P5 未改 executor/worker 代码（P5-03B 仅回复序列化），沿 P4-07 口径登记归属不重复全矩阵；闸门树发现相关回归则升级复验 |
| chaos 等价 | ClusterChaosProcess until-fail 轮次沿 P4-07 口径（≥10 轮全绿即等价 ≥20 chaos 契约延续） |
| 真实依赖 | MySQL/Redis/Kafka 全部真实零 skip |

### 性能矩阵（五元组定位：commit/机器/配置/原始输出/统计脚本）

| 数字源 | 内容 | 定位 |
|---|---|---|
| P5-02 基线 | 9 场景 × 5 重复（connect/echo/slow-consumer + reliable-direct/group/hot/db-backpressure/redis-down/kafka-pause），均值+95% CI+CV | commit 350d4e2；[p5-baseline.md](../reports/performance/p5-baseline.md)；合并产物 `/mnt/d/muduo-chat-build/p5-02-baseline-r1-final.json`；统计脚本 `tests/scripts/p5_02_stats.py` |
| P5-03B 前后对比 | reliable-direct before 35.50 msg/s（CI 33.10..37.90）→ after 42.55（CI 41.03..44.07）= **+19.86%** ≥15%、CI 不重叠、p99 -6.56% 更优、零正确性回退 | commit 3fb46bb；[hotspot-ledger.md](../reports/performance/hotspot-ledger.md) P5-03B 行；产物 `/mnt/d/muduo-chat-build/p5-03b-after-final{,2}.json/.log` |
| P5-03A/C FAIL 如实呈现 | A：-11.5% 无提升（接线复杂度过高未接线）；C：+5.84% <15%（接线落地 N→1 双证据成立但收益面不足）——失败结论入矩阵，不宣称收益 | 台账逐项 commit/revert 可追溯 |
| P5-04 spike 结论 | io_uring 跳过（WSL2 EINVAL）、sendmmsg/recvmmsg 与 MSG_ZEROCOPY 证据不足不开工、Buffer 池化进入条件失效（分配计数 92,406 vs 317,263 = -70.9%，P5-03B 效应）——跳过有据，不宣称未经测量的能力 | HEAD f4ae57f 重测产物 `/mnt/d/muduo-chat-build/p5-04-gprof-*`、探测程序 `/mnt/d/muduo-chat-build/p5-04-spike/iouring_probe.c` |

机器/环境统一标注：WSL2 Ubuntu 24.04 单机（i9-14900HX / kernel 6.6.87.2-microsoft-standard-WSL2）、回环、Debug 构建树、MySQL/Redis/Kafka 本机回环真实依赖。

### 数据性质分类（三类分开标注）

- **synthetic benchmark**：P5-02 九场景、P5-03 各项 before/after 复测、P5-04 分配计数重测与 bench before。
- **故障演练**：P5-01 promtool test rules 断言 + Kafka broker SIGSTOP 真实演练 ×3（firing→resolve）；P5-02 db-backpressure/redis-down/kafka-pause 场景注入；ClusterChaosProcess chaos 契约。
- **真实生产数据**：**无**。本项目无生产流量，全部数字标注「实验环境」，不外推；禁止 SLA/生产承诺措辞（§9.9 停止规则）。

### 四轴对抗审查

Standards / Spec / Concurrency-Fault / Migration-Security，各轴独立审查基于 HEAD f0101e2 + 原始日志；**High/Medium 未决 = 0 才标 M5 VERIFIED**。Spec 轴核对：at-least-once + MessageId 去重语义不变、协议字段/migration 未改、实验 SLO 措辞不升级为 SLA。

### Lows 台账继承复核（逐项登记结论，不影响 VERIFIED 判定）

| # | 继承项 | 来源登记 | 本卡动作 |
|---|---|---|---|
| 1 | db0 presence 键物理累积 | P4-06 环境复原项；P4-07 Lows 复核无回归（harness 断言 TTL != -1 防 EXPIRE 缺失） | 复核无回归 |
| 2 | expired 语义分裂 | M1 登记观察（P4-01 expired 区分 + P4-06 expired 语义登记延续）；P4-07 证据链符合 retention 语义未升级 | 复核是否仍无需升级 |
| 3 | Redis AUTH/TLS 延后 | P4-07 Lows（RESP 有界化已闭环；AUTH/TLS 因本机回环部署边界延后，拓扑外扩需重开） | 复核部署边界未外扩 |
| 4 | 容器分区等价边界 | P4-07 Lows（in-process 无公开 harness seam，网络分区以可控注入模拟；Compose host 网络 + 宿主共享依赖等价覆盖，容器级隔离分区未做） | 复核等价边界声明仍成立 |
| 5 | TSan KafkaEventConsumer topic 卫生 | P4-07 Gate C 台账（默认 topic ~19.6k 积压重放诱发分配器 churn → 析构 T5→T5 同线程堆复用误报；消费组 offset reset latest 为在案口径）；计划 §9.0 bench 前置含 topic 卫生 | Gate C 轮前置 topic 卫生步骤执行并留痕 |
| 6 | legacy contract migration 延后项 | P3-10（contract migration 显式延后至观察窗口关闭；回滚窗口未关不 DROP） | 复核延后状态未变 |
| 7 | MessageAcceptance latency 与 DB pool wait 指标缺口 | P5-01 对抗审查 M-3 登记（dashboard 缺两 panel，根因 = P5-00 /metrics 字段名冻结未暴露二者，延后续卡评估） | 复核缺口现状并如实登记（本卡禁止实现代码改动，不补字段） |
| 8 | 镜像拉取受阻复查 | P5-01 环境决策节登记「P5-05 复查项」：重查 Docker Hub 可达性；若恢复则改 compose 镜像路径验证，tarball 路径留档为 fallback | **本卡执行复查**（探测 Docker Hub registry 可达性一次）并登记结论 |

## RED

- 证据 RED：`docs/reports/p5-m5-gates.md` 不存在（Test-Path False，2026-08-25 开卡核实）——报告不存在即 RED。
- 打回条款：任一对外数字缺五元组定位（commit/机器/配置/原始输出/统计脚本）即被审查打回（§9.7）。
- 命令（闸门树执行时记录）：Gate A/B/D `cmake --fresh -S . -B <tree> ...` + `ctest --test-dir <tree> --output-on-failure`；Gate C 加 `setarch x86_64 -R`；Release `-DENABLE_TESTS=OFF` 构建；focused 抽认沿上表。

## 非目标（本卡不做）

- 不改任何实现代码；不补 MessageAcceptance latency / pool wait 指标字段（属后续卡）。
- 不宣称 SLA/生产承诺/exactly-once；不做生产流量测试。
- 不重写既有报告数字，只汇总引用并对齐五元组；不改 benchmark 方法学（P5-02 冻结，§9.8）。
- 不重跑 P5-02/P5-03 bench（引用既有五元组定位产物；除非闸门树发现数字不可追溯才回对应卡）。

## 完成定义（架构演进方案 §9 M5 行硬条件）

- 每个数字可定位 commit、环境、原始报告和统计脚本。
- 不宣称未经测量的能力。
- 路由四文档（docs/README.md、docs/plans/post-p2-implementation-plan.md、docs/process/implementation-sop.md、docs/process/implementation-progress.md）收口一致。
- 正确性/工具/性能三矩阵 fresh 全绿、真实依赖零 skip；四轴 H/M 未决 = 0 才标 M5 VERIFIED；Lows 台账 8 项逐项有复核结论。

## Commit 与归档

- 提交消息：`docs: publish M5 evidence package`（只含报告/状态/必要文档；四轴 H/M=0 后才 VERIFIED，progress 同步 M5 行）。由提交者执行，本卡 worker 不 stage。
- 随后 P5 任务卡（P5-00..P5-02、P5-03A/B/C/D、P5-04、P5-05）移入 `docs/archive/tasks/p5/`（新建目录，沿 p0/p1/p1r/p2 先例）；移动后跑 Markdown 相对链接检查、`rg` 旧路径扫描（确认无残留 `docs/tasks/P5-` 引用指向已移动文件）、`git diff --check`。

## 开卡执行记录（luna_worker，2026-08-25）

- git rev-parse HEAD = f0101e22df79dda6ebefabff33126805ebf3e2f5（与基线一致）；git log 首行 `experiment(io): record spike skip evidence`。
- git status：仅既有 untracked（`=1`、`Testing/`、bp_server_dbg.log、bp_server_keep.log、docs/process/p3-handoff-2026-08-13.md），无 staged、无 tracked dirty。
- RED 核实：`docs/reports/p5-m5-gates.md` Test-Path = False。
- `docs/archive/tasks/` 现有 p0/p1/p1r/p2，无 p5 目录（归档动作属收口步骤，非开卡）。
- 依赖终态核对：progress P5 表 P5-00/P5-01/P5-02 VERIFIED、P5-03A FAILED/REVERTED、P5-03B PASS/已提交 3fb46bb、P5-03C FAILED/REVERTED、P5-03D CLOSED/评估不实施、P5-04 CLOSED——与计划 §9.7 进入条件一致。
- 路由同步完成（SOP 头部两行、progress P5-05 行），未 stage；`git diff --check` 本卡 scope exit 0。

## 收口执行记录（luna_worker，2026-08-25）

### 五闸门 fresh 数字（闸门树产物自核对，日志均在 /mnt/d/muduo-chat-build/）

- **Gate A Debug**（`m5-final-debug-20260825`）：r1 496/497（618.52s，唯一失败 #120 TcpServerTest.ForceCloseAllWithWorkersIsLoopAffine 瞬时失败）→ flake 定性关闭：同用例隔离 `--repeat until-fail:20` ×20 全绿（`diag-loopaffine-repeat.log`，20 Passed）；r2 计数轮 **497/497**（615.97s）。focused 52/52（75.99s）= SchemaMigrationTest 11 + MessageAcceptanceApplication 16 + ReliableProtocolGolden 14 + DualProtocolCharacterizationTest 2 + DeliveryRetryTest 8 + ReconnectReplayProcess 1（集合不含 ProtocolEncodeContract，如实登记）。
- **Gate B ASan+UBSan**（`m5-final-asan-20260825`）：全量 **497/497**（722.02s）；插桩实证 `loaded.log`（ChatServer __asan×66/__ubsan×32 等）；双处零报告（fullctest 与 lasttest-snapshot 的 AddressSanitizer 计数均 grep = 0）；focused ×20 三目标 **640/640**（ReliableMessageMetricsTest 11 + ReliableProtocolGolden 14 + ProtocolEncodeContract 7 = 32 × 20）。
- **Gate C TSan**（`m5-final-tsan-20260825`，setarch x86_64 -R）：轮前 topic 卫生 v2 执行并留痕——offset reset latest、remaining-laggy-rows=0、日志结尾 **HYGIENE-PASS**（Lows 第 5 项闭环）；libtsan.so.2 加载实证；r1 计数轮 **497/497**（735.37s，exit 0）；双处 0 WARNING（`warningcheck.log`：fullctest 大小写敏感 WARNING=0、LastTest 快照 WARNING=0；2 条 mysql-cli `[Warning]` 为 dbmigrate 工具输出非 TSan 报告）；focused ×20 三目标 **440/440** 0 WARNING（ReliableProtocolGolden 14 + ProtocolEncodeContract 7 + DomainCharacterization 1 = 22 × 20）。
- **Gate D Release tests-off**（`m5-final-release-20260825`）：CMakeCache 实证 Release + ENABLE_TESTS=OFF；构建 exit 0（GCC ICE j2→j1 重试成功）；bin/ChatServer **21987504B**、ChatClient、backfill、chat-bench、dbmigrate、logger-bench、libmymuduo.so、libchatserver_core.a 齐全，bin 无 `*Test` 二进制。
- 真实依赖零 skip；ctest -N = Total Tests 497。

### 四轴对抗审查结论

Standards / Spec / Concurrency-Fault / Migration-Security 四轴独立审查（基于 HEAD f0101e2 + 原始日志）均为 **H/M=0**，满足 §9.7「H/M 未决=0 才标 M5 VERIFIED」；L=4 均为表述精度项并已在报告中落实：

- **L-1**：P5-02 基线 commit 用证据值 **350d4e2**——p5-baseline.md 环境表 / P5-02 卡 / 本卡 Interface 表 / r1-final.json 内嵌 commit 四方一致（已复核 JSON 内嵌值 = 350d4e21d027336e7a561fe345cc500d76715aae），报告未写 c3474a4。
- **L-2**：P5-03B 终态标签用卡面原词「PASS/达标 + 已提交 main @ 3fb46bb」，报告照录未改写。
- **L-3**：direct/group fresh 归属写为「三树全量承载 + ASan focused ProtocolEncode ×20 + TSan focused ProtocolEncode/DomainCharacterization ×20」；Debug focused 52/52 集合如实单列且注明不含 encode seam focused 覆盖。注：卡面 brief 中「Golden 15/DeliveryRetry 7」与日志不符，按日志实测修正为 **Golden 14/DeliveryRetry 8**（总数 52 不变，uniq -c 复核）。
- **L-4**：Lows 第 8 项镜像拉取复查探测结论补登（见下）。

### Docker Hub 探测结论（编排者实测，Lows 第 8 项闭环）

`registry-1.docker.io/v2` 返回 **401**（API 层可达）；`docker pull` 路径仍 **BLOCKED**（docker credential helper `exec format error` 未修复）→ compose 镜像路径验证不具备条件，**tarball fallback 维持**（P5-01 prometheus/grafana tarball 等价运行为在案口径），登记为环境受限持续项；credential helper 或网络恢复后可重开复查。

### 文档收口

- [p5-m5-gates.md](../reports/p5-m5-gates.md) 已产出（沿 p4-m4-gates.md 形态九节：结论/正确性矩阵/工具矩阵/性能矩阵/数据性质分类/四轴/L 项落实/可追溯表/完成判据/Lows 8 项/提交链）。
- progress：P5-05 行 → VERIFIED；里程碑表 M5 行 → VERIFIED（入口 p5-m5-gates.md）。
- SOP 头部：当前活动任务 → 无（P5-05 已 VERIFIED；P5/M5 全部终态）；下一任务 → 无。
- docs/README.md：M5 已 VERIFIED 入口 p5-m5-gates.md；P5 全部终态。
- 提交链 e8ebf55..f0101e2 九提交已在报告登记；本 worker 未 stage、未改实现代码；`git diff --check` 本卡 scope exit 0。
