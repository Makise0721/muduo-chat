# P5-00 Telemetry interface 与基数预算

状态：IN_PROGRESS（2026-08-23 开卡；阶段 A：interface + RecordingTelemetry 契约）
基线：main @ e8ebf5529f5fb107962112eb066e3fb96b41badb（`docs(plan): expand p5 queue into detailed task cards`，git rev-parse HEAD 复核一致）
依赖：P4-07 `VERIFIED`（M4 四轴 H/M=0，progress 索引 2026-08-23）
允许写入：docs/tasks/P5-00.md、docs/process/implementation-sop.md（仅头部活动任务两行）、chatserver/include/app/Telemetry.hpp 与 RecordingTelemetry.hpp（新建，header-only）、tests/unit/TelemetryContractTest.cpp（新建）、tests/CMakeLists.txt（仅注册新测试目标）。未列路径禁止。
不 stage：本卡不执行 `git add/commit`（由提交者负责）；`=1`、`Testing/`、bp_server_dbg.log、bp_server_keep.log、docs/process/p3-handoff-2026-08-13.md 永不 stage。

## 问题与证据

P5 基线事实（计划 §9.0）：指标有三个互不统一的输出面（SIGUSR1 METRICS 行 pool/executor 字段、`reliable_*` 行、LogEvent 结构化日志）；没有统一 Telemetry interface；span 不存在；基数预算只是 ReliableMessageMetrics 模块内约束而非全局 interface 约束。本卡（阶段 A）只建立统一 interface 与其测试 adapter，让"调用面=测试面"成立，为阶段 B 生产导出与缺口接线提供契约。

当前证据（main @ e8ebf55）：

- `chatserver/include/app/Telemetry.hpp` 不存在（glob 核实）。
- 唯一的基数白名单在 `ReliableMessageMetrics.hpp:105-117`（模块内 isDimensionAllowed/requireDimensionAllowed），对其它未来指标无约束力。
- span 无任何实现（rg 'span' 仅文档命中）。

## 设计决定（开卡定案，RED 前冻结）

- **D1 文件布局 = 双 header-only 头**：`app/Telemetry.hpp`（interface + 白名单校验 + Snapshot 类型）与 `app/RecordingTelemetry.hpp`（纯内存测试 adapter，含 Clock 注入）。零 .cpp → chatserver_core 源表零修改（先例：ReliableMessageMetrics.hpp 全 header-only）。生产 adapter 属阶段 B，不在本卡出现。
- **D2 API 形状**（OTel 语义形状的最小子集）：`addCounter(name, delta, labels)`（单调，接口层无递减操作）、`setGauge(name, value, labels)`（last-writer）、`recordHistogramSample(name, valueMs, labels)`、`beginSpan(name, parentSpanId=0, labels)`（返回 recorder 内唯一 spanId，0 保留 = root）、`endSpan(spanId, errored)`。span 属性（trace attrs）本阶段不做——阶段 A 只要求 parent/error 完整可观察。beginSpan 创建/命中 (name,labels) 系列并计入 spansStarted/spansErrored/时长分位数。
- **D3 label 白名单冻结**（§9.8 冻结值清单项；初始集合 = P3-12 已冻结的四低基数维度，新增 key/value 走 §9.8 白名单变更流程）：
  - `delivery_state` ∈ {pending, inflight, acknowledged, expired}（对齐 ReliableMessageMetrics::DeliveryState）
  - `accept_outcome` ∈ {accepted, duplicate, conflict, too_many_recipients}（对齐 AcceptOutcome）
  - `error_class` ∈ {timeout, unavailable, rejected}（初始有界集）
  - `legacy` ∈ {true, false}
  - UserId/MessageId/ClientMessageId/ConnectionId 及其余任意 key 一律拒绝。
- **D4 拒绝语义**：label 违规（key 不在白名单或 value 越界）→ **先 ++rejectedLabels 再抛 invalid_argument**（拒绝计数可见 + 编程错误显式暴露；阶段 B 生产入口经 recordBestEffort 同款守护吞掉异常，计数保留）。系列基数预算（kMaxSeries=64 个不同 (name,labels) 系列）超限 → ++rejectedSeries 并抛。endSpan 未知/重复 spanId → 抛 invalid_argument（沿 ReliableMessageMetrics recordDeliveryTransition unknown identity 先例，不计数）。
- **D5 线程安全策略 = 内部 std::mutex（否决 atomics）**：理由 (a) Snapshot 必须是多字段一致视图，per-field atomics 会撕裂快照；(b) nearest-rank 分位数需排序样本容器，原子化不可行；(c) 与 ReliableMessageMetrics 线程契约同款（其头注释已论证记录面多线程 + 快照面 EventLoop 线程需内部锁）。TSan 设计目标：全部状态访问持同一 mutex_。
- **D6 有界内存**：已完成 span 记录日志上限 kMaxRecordedSpans=4096，超限丢最旧（P3-12 H2 有界内存纪律同款）；histogram 样本按系列保存于 RecordingTelemetry 内部（测试 adapter，规模由测试控制），Snapshot 只输出标量分位数。histogram 与 span 时长样本为测试规模受控类别（阶段 A 测试 adapter 不设样本上限）；阶段 B 生产 adapter 必须实现有界样本预算（登记 P5-00 阶段 B 前置项）。
- **D7 快照惯例**：`SeriesSnapshot` 全标量 POD ×固定数组 `series[kMaxSeries]` + `seriesCount` + `rejectedLabels`/`rejectedSeries` → 整体 standard_layout（static_assert 断言），跨 recorder 逐字段求和聚合（gauge 为 combined-stream last-writer，histogram 分位数按合并样本重算）——沿 ReliableMessageMetrics::Snapshot 惯例。

## Interface / 可观察行为

见 D2/D3/D7 与 tests/unit/TelemetryContractTest.cpp（GREEN 后该文件即冻结用法）。观察面：

1. counter 只增聚合（无递减 API；多次 addCounter 累加）。
2. gauge last-writer。
3. histogram nearest-rank 分位数（p50/p95/p99 = ceil(p*n) 1 基索引，空集全 0），与 ReliableMessageMetrics 既有语义一致。
4. span parent（parentSpanId 字段）/error（errored 字段）/时长（注入 Clock 差值）完整可观察（recordedSpans()）。
5. 高基数 label 注入被拒且 rejectedLabels 计数可见。
6. 快照跨 recorder 逐字段聚合正确；跨线程并发记录后总量精确。
7. label key 白名单 + 每 key 有界枚举 value 集。

## 非目标（阶段 B 内容，本卡不做）

- 生产 adapter（Prometheus `/metrics` 端点、SIGUSR1 统一快照同源生成函数、span 结构化日志 sink）。
- 缺口指标接线（EventLoop lag、connection accept/reject reason、outstanding bytes、presence fencing conflicts、consumer lag/rebalance）。
- ReliableMessageMetrics 桥接进统一快照（冻结契约零修改）。
- mymuduo 任何改动；既有生产文件任何行为修改。
- span trace 属性、采样与保留预算实现（trace 属性高基数豁免是阶段 B+ 事项）。
- OTel SDK 引入（延后条款见计划 §9.2 开头修订）。

## RED

- 测试：`tests/unit/TelemetryContractTest.cpp`（新建，注册进 tests/CMakeLists.txt；引用尚不存在的 `app/Telemetry.hpp`/`app/RecordingTelemetry.hpp` → 编译失败即合法 RED，沿 P3-12/P4-01..P4-05 先例）。
- 用例清单（RED 前冻结）：CounterAccumulatesMonotonically / GaugeRecordsLastWriterValue / HistogramNearestRankQuantiles / SpanCarriesParentAndErrorState / HighCardinalityLabelsRejectedAndCounted / LabelWhitelistBoundedEnumValues / SnapshotAggregatesAcrossRecorders / SnapshotCrossThreadAggregationCorrect / SeriesBudgetRejectsOverflowVisibly。
- 命令：`wsl.exe -d Ubuntu -- bash -lc "cmake -S /mnt/d/agent_learning/muduo-chat -B /mnt/d/muduo-chat-build/p5-00a-debug -DCMAKE_BUILD_TYPE=Debug -DENABLE_TESTS=ON && cmake --build /mnt/d/muduo-chat-build/p5-00a-debug --target TelemetryContractTest -j2"`
- 预期失败：`app/Telemetry.hpp: No such file or directory` 编译失败，退出码非 0。
- 实际失败与退出码：exit 2（`app/Telemetry.hpp: No such file or directory`，TelemetryContractTest.cpp:30，合法 RED，日志 p5-00a-red-build.log）。

## 最小实现

- `chatserver/include/app/Telemetry.hpp`：Label 聚合、kMaxSeries、is/require LabelKeyAllowed/LabelValueAllowed、TelemetrySink 抽象、SeriesSnapshot/Snapshot（standard layout）。
- `chatserver/include/app/RecordingTelemetry.hpp`：TelemetrySink 纯内存实现（Clock& 注入、内部 mutex_、系列表 ≤kMaxSeries、span 表、完成 span 有界日志、rejectedLabels/rejectedSeries 计数、snapshot()/seriesIndexOf()/recordedSpans() 观察面）。
- `tests/CMakeLists.txt` 注册 TelemetryContractTest（target + CROSSCOMPILING_EMULATOR 列表 + discover_gtest）。

## 验证

- 聚焦：`ctest --test-dir /mnt/d/muduo-chat-build/p5-00a-debug -R 'Telemetry' --output-on-failure`。
- Debug 全量：fresh configure `/mnt/d/muduo-chat-build/p5-00a-debug` 全量 CTest（MySQL integration 需真实库，DB_PASSWORD 环境已配置则零 skip；依赖失败即停止报告）。
- ASan+UBSan / TSan：本阶段为纯单测新增（零生产接线、零 .cpp），由编排者闸门树执行（阶段 A 卡内不单独跑 sanitizer 全量，登记为闸门归属）。
- `git diff --check` exit 0；diff 只含允许范围。

## Diff 审查

- 四轴审查：H=0/M=0（对抗审查见下节摘要）。
- `git diff --check` exit 0（无尾随空白）。
- diff 仅含允许范围：chatserver/include/app/Telemetry.hpp、RecordingTelemetry.hpp（新建）、tests/unit/TelemetryContractTest.cpp（新建）、tests/CMakeLists.txt（仅注册新测试目标）、docs/tasks/P5-00.md、docs/process/implementation-sop.md（仅头部活动任务两行）。

## 完成定义

- 测试只穿过 TelemetrySink 公开 interface 与 RecordingTelemetry 公开观察面，不读私有容器、不依赖 sleep（时间相关用注入 FakeClock）。
- 高基数 label 注入被拒 + reject 计数有测试拦截（§9.9 停止规则的 P5-00 对应防线）。
- 快照 standard layout 且跨 recorder/跨线程聚合精确。
- 零生产接线、零导出面、零 mymuduo 改动。

## Commit

`feat(observability): add telemetry contract and recording adapter`（提交边界一：interface+recorder+contract 测试；由提交者执行，本卡 worker 不 stage）

## 阶段 A 验证记录（2026-08-24）

- **RED**：命令 `wsl.exe -d Ubuntu -- bash -lc "cmake -S ... -B /mnt/d/muduo-chat-build/p5-00a-debug ... --target TelemetryContractTest -j2"`；退出码 exit 2，原因 `app/Telemetry.hpp: No such file or directory`（TelemetryContractTest.cpp:30，引用尚不存在的头 → 合法 RED）。日志 p5-00a-red-build.log。
- **GREEN**：聚焦 9/9 全绿——CounterAccumulatesMonotonically / GaugeRecordsLastWriterValue / HistogramNearestRankQuantiles / SpanCarriesParentAndErrorState / HighCardinalityLabelsRejectedAndCounted / LabelWhitelistBoundedEnumValues / SnapshotAggregatesAcrossRecorders / SnapshotCrossThreadAggregationCorrect / SeriesBudgetRejectsOverflowVisibly。
- **断言自洽修正**：`SnapshotAggregatesAcrossRecorders` 的 seriesCount 期望由 3 修正为 4、`SnapshotCrossThreadAggregationCorrect` 的 seriesCount 由 3 修正为 4、histogram 断言跨 recorder 修正为 ihA/ihB/ihAB 三系列区分——修正后 9/9 自洽通过（原 8/9，1 项断言与实现不一致）。
- **对抗审查摘要**：H=0 / M=0 / L=5 —— L-1（D2）beginSpan 创建/命中系列语义澄清并计入 spansStarted/spansErrored/时长分位数（已补记 D2）；L-2 多 label 顺序测试可选（不阻塞）；L-3（D6）histogram 与 span 时长样本有界性补注 + 阶段 B 有界登记（已补记 D6）；L-4 `kNoSeries` 枚举值偏离已文档化（记录，不阻塞）；L-5 命名空间风格可选（不阻塞）。
- **Gate A**：fresh Debug 全量 CTest 469/469（506.82s，Kafka 真实连通零 skip；日志 p5-00a-final-debug-20260824-p5-00a-fullctest.log）。
