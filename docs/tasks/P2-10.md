# P2-10 M2 独立验收与性能矩阵

状态：VERIFIED
基线：main @ e6d2671（P2-09 后）
提交：`feat(metrics): server metrics snapshot + chat load bench`、`docs: verify M2 application and reactor gates`

## 实现

- SIGUSR1 → `METRICS pool_total/idle/active executor_queue/drop_full/drop_shutdown`
  （BlockingExecutor 增 queueDepth/droppedFull/droppedShutdown；ChatService 透传；
  main 信号回调按字节区分 USR1/INT/TERM，多信号合并逐字节处理）；
- tools/chat_load.py：N 并发 × reg→login→离线单聊循环，吞吐 + p50/p95/p99；
- **chat-bench 修复**：slow-consumer 阻塞 recv 无视 duration → SO_RCVTIMEO 100ms +
  EAGAIN continue（回到 deadline 检查）。

## 验证矩阵（2026-08-10 实测，报告 docs/P2-10_M2_GATES.md）

1. 性能矩阵（16 并发 chat 负载 15s / connect 200 / slow-consumer 4×1.28MB，1/2/4/8 loops）：
   chat msg/s = 101.8/118.5/123.8/122.8（饱和 ~123，瓶颈=executor 单 worker 串行
   DB 写 ~8ms）；executor_queue 恒 15、drop=0、pool_active=1；connect p99 =
   410/483/224/326us；slow-consumer 全档 sent=80000 recv=0 early_closes=4（背压无
   阻塞）；
2. BlockingExecutorTest metrics 2/2（队列深度、drop 计数、inline 零值）+ server-metrics
   进程测试（METRICS 行格式 + 干净退出）；
3. M2 矩阵全绿：Debug/ASan/TSan 183/183（180 + 3 新增）；Release
   `-DENABLE_TESTS=OFF` 全新构建通过；MySQL 套件不 skip（P2-04 起）；
4. git diff --check exit 0。

## 决策：executor 保持单 worker

- 数据：1→2 loops +17%，2→4 +4%，4→8 持平；队列恒 15 积压（未满，drop=0）；
  p50~125-150ms = 16 并发 × 串行 8ms DB 写排队延迟，数学吻合。
- 扩并需按用户分片保同连接串行依赖（P2-06 教训）；分片队列属架构工作，留 P3
  语义（消息/投递建模）后评估。本报告记录上限，不隐含生产容量。
