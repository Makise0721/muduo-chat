# 文档归档

这里保存已经完成且不再作为当前执行指令的证据。

- `tasks/p0/`：可测试基线、生命周期与 benchmark 基础。
- `tasks/p1/`：网络内核生产化的原始任务卡。
- `tasks/p1r/`：P1 完成性审查后的整改与独立验收。
- `tasks/p2/`：应用 seam、Repository、executor、多 Reactor 和 M2 验收。
- `design/evolution-plan-original.md`：最初 brainstorm 整理出的 P0–P5 全量方案与历史任务清单。

归档文件是不可变的历史快照。它们可能引用文件移动前的路径，这些路径应按“执行时事实”理解，不做批量现代化改写。当前计划、命令和状态请从 [文档导航](../README.md) 进入。
