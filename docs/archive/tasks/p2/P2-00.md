# P2-00 固化领域词汇与现有行为

状态：VERIFIED
基线：main @ 4cc50b3（P1R-09 后）
提交：`test(domain): characterize chat use cases`

## 接口/行为

- 新增领域词汇表 `CONTEXT.md`：Command/Reply/Session/User/Friendship/Group/
  GroupRole/Message/Delivery/Errno 的定义与 _Avoid_；明确"不把 JSON 字段名
  （msgid/errno/id）直接充当领域模型"。
- 新增行为矩阵 `docs/DOMAIN_BEHAVIOR_MATRIX.md`：B-01..B-26 每条行为定位到代码
  行；类别"稳定/待ADR"标注与 P2 目标冲突的现状（B-14/18/19/21/22/23/24/25/26）。
- 测试 fixture（不迁移生产用例）：
  - unit `DualProtocolCharacterizationTest`：v1/v2 同一 payload round-trip 还原同一
    JSON（业务等价前置）+ 超限仅 v2 拒绝（B-23）；
  - 进程 `DomainCharacterization`（CTest）：真实服务器 + 真实 MySQL，v1/v2 双连接
    跑 29 项检查，逐项断言一致。

## 过程（RED → GREEN）

- RED-1：首个脚本版注册/登录/登出链——登出用 msgid=2 → "can not find handler"
  （LOGINOUT_MSG=3），且服务器对非数字 id（脚本误传名字）抛未捕获异常
  **terminate + core dump**（暴露 B-25）；错密码步骤误用正确密码（断言错）；
  超长消息在宽松 sql_mode 下截断入队成功（B-13 依赖环境）。
- GREEN：修正 msgid=3、错密码显式构造、删除 sql_mode 依赖的 D5 断言；
  捕获 ConnectionResetError（T1 forceClose RST）。
- 全程 Debug 108/108、ASan 108/108、TSan 108/108；DomainCharacterization
  --repeat until-fail:2 稳定。

## 验证矩阵（2026-08-10 实测）

1. `ctest -R 'DomainCharacterization|DualProtocol'` 3/3；
2. 全量 Debug/ASan/TSan 108/108（105 存量 + 3 新增）；
3. 进程矩阵 MATRIX_ALL_PASS（29 检查：防御/注册/重名/登录/错密码/重复登录/登出
   幂等/离线补投/单聊在线离线/好友/好友列表/建群/入群/群聊/非成员群聊/断开重连/
   v2 超限关连接，v1/v2 逐项一致）；
4. 文档：git diff --check exit 0。

## 完成定义

- v1/v2 对同一矩阵结果一致：**达成**（29 项双协议断言）；
- 每条行为能定位到当前代码或明确标为待办 ADR：**达成**（B-01..B-26 全部带
  代码定位；B-13/14/18/19/21/22/23/24/25/26 标"待ADR"）；
- 只新增领域词汇/行为矩阵/测试 fixture，不迁移生产用例：**达成**
  （零生产代码改动，提交仅含 docs + tests + CMake 注册）。

## Commit

`test(domain): characterize chat use cases`
